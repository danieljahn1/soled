{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/soled_backend/soled_backend.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}